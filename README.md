# Simple SvelteKit Todo

This is a simple Todo application built with SvelteKit and Typescript. It leverages the localStorage API for data persistence and the you can export the data to JSON files.

## Running / Building

To build a production version of the application as a static site:
`pnpm i && pnpm run build`


To run a development version:
`pnpm i && pnpm run dev`


